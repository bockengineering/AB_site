{
  "name": "dynamic-site",
  "version": "1.0.0",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && npm install && node validate-env.js && NODE_ENV=production node server.js",
    "dev": "nodemon server/index.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd client && npm install",
    "seed-reads": "node scripts/seed-reads.js",
    "validate-env": "cd server && node validate-env.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
